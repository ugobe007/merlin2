feat: industry-driven generator logic — hospitals, data centers, airports, manufacturing auto-include generator

Philosophy change (Robert, April 2026): BESS + generator is the primary package
for real commercial/industrial quotes. Solar is the optional add-on when roof/land
space permits. Vineet's feedback was biased toward solar-first; this corrects that.

Changes:
- addonSizing.ts: add GENERATOR_DEFAULT_INDUSTRIES Set (hospital, data_center,
  airport, manufacturing) + industryRequiresGenerator(industry?) helper
  exported from the shared constants file. criticalLoadPct threshold intentionally
  NOT included here — stays in the if_critical policy gate only.

- step4Logic.ts computeGeneratorKW:
  * industryGen = industryRequiresGenerator(state.industry) overrides if_requested
    policy so these industries always get a generator in the quote
  * explicitlyDeclined = wantsGenerator === false — user autonomy respected even
    for mandated industries (if they remove it in Step 3.5, we honor that)
  * Starter-tier guard: critical industries now get generator in Starter tier too

- step4Logic.ts applyPaybackGuardrail:
  * generatorIsExplicitlyNeeded uses industryRequiresGenerator() + criticalLoadPct>=0.5
    separately — ROI guardrail never strips generator from these facilities

- Step3_5V8.tsx:
  * wantsGenerator useState: defaults ON for GENERATOR_DEFAULT_INDUSTRIES
  * useEffect first-visit: auto-enables generator card for mandated industries
    (setAddonPreference + setAddonConfig with recGenKW)

- wizardCalcWorkflow.test.ts:
  * Updated hospital test: "NOT auto-included" → "auto-included by mandate"
  * Added new test: explicit wantsGenerator=false overrides mandate

43 test files / 2704 passing / 7 skipped / 0 failures / tsc clean
