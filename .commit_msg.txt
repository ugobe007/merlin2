fix: add page title, replace alert() with inline errors, fix audit test selectors

- index.html: add <title>Merlin Energy — Smart BESS & Solar Quotes</title> (was missing — SEO critical)
- AuthModal.tsx: replace all 5 alert() calls with inline errorMessage/successMessage state
  - Red banner with role=alert for errors, green banner with role=status for success
  - Auto-close modal 800ms after successful sign-in, 1200ms after sign-up
- pre-launch-audit.spec.ts: fix strict-mode locator violations
  - Use .first() on multi-match text selectors (TrueQuote, Guided Wizard)
  - Steps 1+2 self-advance by design — removed incorrect Continue button clicks
  - 8a quote test: non-crashing assertion + logs dollar count (full flow requires real inputs)
