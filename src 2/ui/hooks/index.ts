/**
 * UI Hooks - Barrel Export
 * =========================
 * Central export point for all UI layer hooks.
 * 
 * These hooks provide React components with clean interfaces to:
 * - Application workflows
 * - State management
 * - Side effects
 * 
 * Usage:
 * import { useQuoteBuilder } from '@/ui/hooks';
 */

export { useQuoteBuilder } from './useQuoteBuilder';
