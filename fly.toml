# fly.toml app configuration file for Merlin BESS Quote Builder
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#
# IMPORTANT: This must match your Fly.io app name
# Current app: merlin2 (main production app)
# Alternative apps: merlin (old), merlin-bess-quote-builder (testing)

app = 'merlin2'
primary_region = 'lax'

[build]
  [build.args]
    # Public Supabase credentials (anon key is designed to be public — shipped in browser JS)
    VITE_SUPABASE_URL = "https://fvmpmozybmtzjvikrctq.supabase.co"
    VITE_SUPABASE_ANON_KEY = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImZ2bXBtb3p5Ym10emp2aWtyY3RxIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjIyODIyOTAsImV4cCI6MjA3Nzg1ODI5MH0.ACqSuHx_-uvrK6-e0sXQO5AmHlA2K0BQUIT3dMRQS_0"
    # Other VITE_ vars passed via --build-arg in CI (from GitHub secrets)

[http_service]
  internal_port = 80
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
